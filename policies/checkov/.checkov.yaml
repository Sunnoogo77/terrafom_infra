quiet: true
compact: true
framework: terraform


# Exemple : si tu veux ignorer certains checks plus tard
skip-check:
  # - CKV_AZURE_XXX   # (à remplir quand tu verras un faux positif)
  # - CKV2_AZURE_YYY
  # StorageV2 does not support classic "Storage Analytics" blob logging.
  # We use Diagnostic Settings to Log Analytics instead (see modules/storage).
  - CKV2_AZURE_21

  # SQL Vulnerability Assessment provisioning is non-trivial (requires dedicated storage config).
  # We'll implement it in a dedicated iteration; keep CI unblocked meanwhile.
  - CKV2_AZURE_2

# Scan both environment and modules.
directory:
  - envs

#test ---- 